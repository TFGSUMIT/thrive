// chat module UI registration — a simple thrive-native chat over local models
// (LM Studio), with a model switcher. The full open-webui lives at brain.n.com.
import ChatPage from './ChatPage'

export default {
  id: 'chat',
  path: '/chat',
  Page: ChatPage,
}
