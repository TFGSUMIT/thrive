import GroceriesPage from './GroceriesPage'

// Groceries module — a shared household shopping list. Opt-in nav page.
export default {
  id: 'groceries',
  path: '/groceries',
  Page: GroceriesPage,
}
