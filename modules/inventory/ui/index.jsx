import InventoryPage from './InventoryPage'

// Inventory module — a home inventory of possessions (value, warranties, by room).
export default {
  id: 'inventory',
  path: '/inventory',
  Page: InventoryPage,
}
