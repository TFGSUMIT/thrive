import PantryPage from './PantryPage'

// Pantry module — what's on hand in the fridge/freezer/pantry, with an optional
// one-tap send to the Groceries list when that module is active.
export default {
  id: 'pantry',
  path: '/pantry',
  Page: PantryPage,
}
