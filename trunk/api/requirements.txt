# Core platform deps ONLY. Module backends declare their own deps in
# modules/<name>/requirements.txt, installed at API image build time (see
# core/api/Dockerfile) — so core stays lean and module-agnostic.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
# cryptography (crypto.py / personal-secret encryption) is declared by the modules
# that use it — connections + steam — so core stays lean when they're not installed.
